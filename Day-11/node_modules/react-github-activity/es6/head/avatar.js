'use strict';

import React from 'react';

export default function (props) {
  return React.createElement(
    'div',
    { style: props.styles.githubAvatar },
    React.createElement('img', { style: props.styles.githubAvatarImg, src: props.avatarUrl })
  );
}